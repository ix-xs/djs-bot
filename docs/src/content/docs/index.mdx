---
title: djs-bot
description: A TypeScript-first, production-ready framework for Discord bots built on discord.js.
template: splash
hero:
  tagline: Write features, not plumbing. Your bot is production-ready on day one - and you can always read exactly what the framework does.
  actions:
    - text: Build your first bot
      link: /djs-bot/tutorial/what-you-need/
      icon: rocket
      variant: primary
    - text: Browse the guide
      link: /djs-bot/guide/basics/install-project-layout/
      icon: open-book
      variant: minimal
    - text: GitHub
      link: https://github.com/ix-xs/djs-bot
      icon: github
      variant: minimal
---

import { Card, CardGrid, LinkCard, Tabs, TabItem, Aside } from '@astrojs/starlight/components';
import DiscordMessage from '../../components/DiscordMessage.astro';
import DiscordEmbed from '../../components/DiscordEmbed.astro';
import DiscordButtons from '../../components/DiscordButtons.astro';

## From this, to this

<Tabs>
  <TabItem label="A command">
    ```ts
    import { defineCommand, s } from "@ix-xs/djs-bot";

    export default defineCommand({
      name: "profile",
      description: "Show a player profile",
      options: {
        user: s.user({ description: "Whose profile", required: true }),
      },
      run: (ctx) => ctx.reply.success(`Profile of ${ctx.options.user}`),
      //                                   ctx.options.user is a User, never undefined
    });
    ```
    <DiscordMessage author="djs-bot">
      <DiscordEmbed color="#23a55a" description="✅ Profile of @Alice" />
    </DiscordMessage>
  </TabItem>
  <TabItem label="A button">
    ```ts
    import { defineButton, p, ButtonStyle } from "@ix-xs/djs-bot";

    export const CloseTicket = defineButton({
      id: "ticket:close",
      params: { ticketId: p.string, ownerId: p.string },
      run: async (ctx) => {
        // ctx.params is decoded and fully typed - no split("_")
        if (ctx.user.id !== ctx.params.ownerId) return void ctx.reply.error("Not your ticket.");
        await ctx.update.disable();
      },
    });
    ```
    <DiscordMessage author="djs-bot" content="🎫 <strong>Printer is on fire</strong>">
      <DiscordButtons buttons={[{ label: "Close ticket", style: "danger", emoji: "🔒" }]} />
    </DiscordMessage>
  </TabItem>
  <TabItem label="An event">
    ```ts
    import { defineEvent } from "@ix-xs/djs-bot";

    export default defineEvent("guildMemberAdd", async (member, ctx) => {
      // The GuildMembers intent is enabled automatically because of this line
      await member.guild.systemChannel?.send(`Welcome ${member}! 👋`);
    });
    ```
    <DiscordMessage author="djs-bot" content="Welcome @NewUser! 👋" />
  </TabItem>
</Tabs>

## Why djs-bot

<CardGrid stagger>
  <Card title="Type-safe end to end" icon="approve-check">
    Typed slash options, typed `customId` params, typed services and config. `required: true` makes an option non-optional - no casts, no `any`.
  </Card>
  <Card title="No magic" icon="setting">
    Files export `define*()` objects; nothing registers by importing. `djs-bot explain` shows exactly what's loaded and why.
  </Card>
  <Card title="Production-ready" icon="rocket">
    Error boundaries, graceful shutdown, structured logs, sharding, health checks, i18n, persistence and rate limiting - built in.
  </Card>
  <Card title="Batteries included" icon="puzzle">
    Subcommands, context menus, autocomplete, native selects, triggers, pagination, `confirm()`, presence, assets, voice - and official plugins.
  </Card>
</CardGrid>

## Get started in 60 seconds

```bash
npm install @ix-xs/djs-bot discord.js
npx djs-bot init      # scaffold a working starter
npx djs-bot dev       # watch + instant deploy to your test server
```

<Aside type="tip" title="Never built a bot before?">
The [tutorial](/djs-bot/tutorial/what-you-need/) walks you through everything -
creating the app on Discord, getting your token, inviting the bot, your first
command, buttons, and putting it online. No prior experience assumed.
</Aside>

## Explore

<CardGrid>
  <LinkCard title="Tutorial" description="Zero to a deployed bot, step by step." href="/djs-bot/tutorial/what-you-need/" />
  <LinkCard title="Commands & options" description="Typed options, subcommands, context menus, guards." href="/djs-bot/guide/commands/slash-commands-typed-options/" />
  <LinkCard title="Components" description="Typed buttons, selects, modals, Components V2, pagination." href="/djs-bot/guide/components/typed-buttons-the-customid-router/" />
  <LinkCard title="Architecture" description="Services & DI, jobs, plugins, reusable features." href="/djs-bot/guide/architecture/services-dependency-injection/" />
  <LinkCard title="Data & state" description="Cache, i18n, key-value persistence, feature flags." href="/djs-bot/guide/data/persistence-key-value-stores/" />
  <LinkCard title="Production" description="Deploy, CLI, sharding, resilience, audit, health checks." href="/djs-bot/guide/ops/deploying-global-specific-guilds/" />
</CardGrid>

## Complete API reference

Every export, every option, every error code - written by hand, not generated.

<CardGrid>
  <LinkCard title="API overview" description="The full export map: what exists and where it is documented." href="/djs-bot/api/overview/" />
  <LinkCard title="Configuration & env" description="defineBot, every BotConfig field, env() and env.optional()." href="/djs-bot/api/configuration/" />
  <LinkCard title="The ctx object" description="reply, update, options, params, fields, services, logger, t, audit." href="/djs-bot/api/context/" />
  <LinkCard title="Plugins & hooks" description="Official plugins, every lifecycle hook, writing your own." href="/djs-bot/api/plugins/" />
  <LinkCard title="Store, i18n, flags, audit" description="The runtime services, method by method." href="/djs-bot/api/store/" />
  <LinkCard title="Error codes" description="Every DJSBOT_Exxx: what causes it, how to fix it." href="/djs-bot/api/errors/" />
</CardGrid>
